#include "main.h"

/**
 * _abs - get the absoulute value of number
 * @n: the number to get the absolute value of
 * Return: the absolute value of number
*/
int _abs(int n)
{
	return ((n < 0) ? n * -1 : n);
}
